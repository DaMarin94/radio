import type { RadioStation } from "@radio/shared";

// TODO: re-enable polling once ICY + AudD integration is complete
export function useNowPlaying(_station: RadioStation | null): string | null {
  return null;
}
